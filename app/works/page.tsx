/** Add your relevant code here for the issue to reproduce */

export const metadata = {
	title: "This will be visible if shared via Slack, X, or Facebook",
	description: "This will also be visible if shared via Slack, X, or Facebook",
};

export default function Home() {
	return <>hello world</>;
}
